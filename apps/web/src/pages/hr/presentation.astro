---
/*
 * Croatian moved to the root (revision decision R1). The old address keeps
 * working as a permanent redirect, query string and all, so `?present=1` still
 * lands in presenter mode.
 */
return Astro.redirect(`/presentation${Astro.url.search}`, 301);
---
