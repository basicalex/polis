---
import Base from '../layouts/Base.astro';
---
<Base title="Verify"><section class="panel"><h1>Verify</h1><p>The BFF currently supports `POST /api/v1/verify/hash` for request-local SHA-256 verification. File upload, persisted manifests, timestamps, and signature verification are later milestones.</p></section></Base>
